<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :评论人 %><br>
    <%= f.text_field :commenter, size: 35 %>
  </p>
  <p>
    <%= f.label :评论 %><br>
    <%= f.text_area :body, size: "40x10" %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>